spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/hanq?useSSL=false&serverTimezone=Asia/Seoul
    username: root
    password: hanq1234!
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    show-sql: true
server:
  port: 8080

springdoc:
  api-docs:
    path: /v3/api-docs         # Swagger 문서 JSON 경로
  swagger-ui:
    path: /swagger-ui.html     # Swagger UI 웹 경로

